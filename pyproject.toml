[project]
name = "kubera"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KUBERA Agents", email = "agents@kubera.local" }
]
requires-python = ">=3.14.7"
dependencies = [
    "kokoro-onnx>=0.5.0",
    "soundfile>=0.14.0",
]

[project.scripts]
kubera = "kubera:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
