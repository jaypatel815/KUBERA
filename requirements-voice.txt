# Voice loop dependencies (scripts/talk.py) — separate from backend/requirements.txt
# so CI and the API server stay lean. Install on the owner's machine only:
#   pip install -r requirements-voice.txt
sounddevice>=0.4
numpy>=1.26
pyttsx3>=2.90
# Local speech-to-text (default). If wheels are unavailable for your Python version,
# skip it and set KUBERA_STT=openai (uses OPENAI_API_KEY) instead:
faster-whisper>=1.0
# Optional nicer voices — pick ONE, set KUBERA_TTS accordingly:
#   KUBERA_TTS=kokoro (RECOMMENDED, D024: near-human, free, offline — text stays local)
#   pip install kokoro-onnx
#   Download kokoro-v1.0.onnx + voices-v1.0.bin from:
#     https://github.com/thewh1teagle/kokoro-onnx/releases
#   Place in models/kokoro/ or set KUBERA_KOKORO_DIR to their folder.
#
#   KUBERA_TTS=edge   (free, Microsoft neural, online — reply text leaves machine)
#   pip install edge-tts soundfile
#
#   KUBERA_TTS=openai (near-human, ~$0.015/1k chars, OPENAI_API_KEY required — reply text leaves machine)
#   pip install openai soundfile
